backend:
  name: github
  repo: gooddeal22/dailymusicgems
  branch: main
  base_url: https://dailymusicgems.pages.dev
  auth_endpoint: api/auth
  # NOTE: temporarily pointed at the .pages.dev domain while dailymusicgems.xyz's
  # DNS is still being cut over to Cloudflare. Switch this back to
  # https://dailymusicgems.xyz once the custom domain is connected.

# This tells the CMS to open a real GitHub login, handled by our own OAuth
# endpoints running as Cloudflare Pages Functions (functions/api/auth.js and
# functions/api/callback.js) — this replaces Netlify's built-in OAuth bridge.
# Only GitHub accounts you've given push access to your repo can log in and publish.

media_folder: "content/uploads"
public_folder: "/uploads"

collections:
  - name: "posts"
    label: "Articles"
    label_singular: "Article"
    folder: "content/posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Artist", name: "artist", widget: "string", required: false, hint: "The artist this piece is about. Used to build the alphabetical 'By Artists' index." }
      - label: "Category"
        name: "category"
        widget: "select"
        options:
          - "Style Analysis"
          - "Discography Deep-Dive"
          - "Artist Evolution"
          - "Album Review"
          - "Other"
      - { label: "Publish Date", name: "date", widget: "datetime", format: "YYYY-MM-DD", picker_utc: true }
      - { label: "Excerpt", name: "excerpt", widget: "text", required: false, hint: "One-line summary shown on the homepage card. Auto-generated from the body if left blank." }
      - { label: "Featured Image", name: "image", widget: "image", required: false, hint: "Shown at the top of the article and on its preview card." }
      - { label: "Featured", name: "featured", widget: "boolean", default: false, required: false, hint: "Check this to include the piece in the homepage 'Featured' picks. You choose which pieces count as featured." }
      - { label: "Body", name: "body", widget: "markdown" }
